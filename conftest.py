# Present so pytest treats this folder as the rootdir and puts it on sys.path,
# making `import commandcenter` resolve when the L3 reproduction/test-runner runs the suite from anywhere
# (mirrors samples/shopfront + samples/chatham-direct).
